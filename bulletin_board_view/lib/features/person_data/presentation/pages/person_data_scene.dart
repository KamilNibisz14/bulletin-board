import 'package:flutter/material.dart';

class PersonDataScene extends StatelessWidget {
  const PersonDataScene({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'Found'
        )
      ),
    );
  }
}